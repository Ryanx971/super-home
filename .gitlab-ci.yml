image: node:16

stages:
  - test

unit-test:
  stage: test
  before_script:
    - pnpm install
  script:
    - pnpm run test:ci
  coverage: /All files[^|]*\|[^|]*\s+([\d\.]+)/
  artifacts:
    paths:
      - coverage/
    when: always
    reports:
      junit:
        - junit.xml

